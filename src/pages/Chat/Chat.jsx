import s from './Chat.module.css';

const Chat = () => {
  return (
    <div className={s.mainPage}>
      <h1 className={s.title}>Select the contact you want to chat with!</h1>
    </div>
  );
};

export default Chat;
